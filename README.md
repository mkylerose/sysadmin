user_audit.sh --	Lists all users, login shells, last login time, and sudo privileges.
disk_health_report.sh -- Checks disk usage, SMART status, and inode usage.
service_monitor.sh -- Monitors a list of services and restarts them if down.
package_update_notifier.sh -- Checks for and emails/alerts you of available system updates.
logwatcher.sh -- Tails and filters logs for keywords (e.g., "error", "failed login").
